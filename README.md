# 🔐 Система аутентификации и авторизации (RBAC)

Backend-приложение с собственной системой аутентификации и авторизации на основе ролей (RBAC). Реализовано на Django + Django REST Framework.

## 📋 Требования

- Python 3.10+
- Django 4.2+
- Django REST Framework
- SQLite (или PostgreSQL)

## 🚀 Быстрый старт

### 1. Клонирование и установка

```bash
git clone <ваш-репозиторий>
cd auth_system

# Создание виртуального окружения (опционально)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка базы данных

```bash
# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser
# Email: admin@example.com
# Password: admin123
# Имя: Admin
# Фамилия: Adminov

# Загрузка тестовых данных (роли и разрешения)
python manage.py loaddata fixtures/initial_data.json
```

### 3. Запуск сервера

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: http://127.0.0.1:8000/

## 📊 Тестовые пользователи

| Email | Пароль | Роль | Права |
|-------|--------|------|-------|
| `admin@example.com` | `admin123` | Администратор | Все права (включая `permission.manage`) |
| `simple@example.com` | `simple123` | Пользователь | `article.read` |

## 🏗️ Архитектура системы

### Система RBAC (Role-Based Access Control)

```
Пользователь (User) → Роль (Role) → Разрешения (Permissions)
```

#### Модели:
1. **User** - кастомная модель пользователя
2. **Role** - роли (Администратор, Модератор, Пользователь)
3. **Permission** - разрешения вида `ресурс.действие`
4. **RolePermission** - связь многие-ко-многим

#### Примеры разрешений:
- `article.read` - чтение статей
- `article.create` - создание статей  
- `article.update` - редактирование статей
- `article.delete` - удаление статей
- `user.read` - просмотр пользователей
- `user.update` - редактирование пользователей
- `permission.manage` - управление правами и ролями (только для администраторов)

## 🔌 API Endpoints

### Аутентификация

| Метод | Endpoint | Описание | Требуемые права |
|-------|----------|----------|-----------------|
| POST | `/api/register/` | Регистрация | Нет |
| POST | `/api/login/` | Вход в систему | Нет |
| POST | `/api/logout/` | Выход из системы | Нет |
| GET | `/api/profile/` | Профиль пользователя | `user.read` |
| PUT/PATCH | `/api/profile/update/` | Обновление профиля | Аутентификация |
| DELETE | `/api/profile/delete/` | Мягкое удаление | Аутентификация |

### Mock API (Статьи)

| Метод | Endpoint | Описание | Требуемые права |
|-------|----------|----------|-----------------|
| GET | `/api/articles/` | Список статей | `article.read` |
| POST | `/api/articles/create/` | Создание статьи | `article.create` |
| GET | `/api/articles/{id}/` | Детали статьи | `article.read` |
| PUT/PATCH | `/api/articles/{id}/update/` | Обновление статьи | `article.update` |
| DELETE | `/api/articles/{id}/delete/` | Удаление статьи | `article.delete` |

### Admin API (Управление правами)

**Требуется разрешение: `permission.manage` (только для администраторов)**

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/admin/roles/` | Список всех ролей |
| GET | `/api/admin/roles/{id}/` | Детали роли с разрешениями |
| GET | `/api/admin/permissions/` | Список всех разрешений |
| POST | `/api/admin/roles/{id}/permissions/` | Добавить разрешение роли |
| DELETE | `/api/admin/roles/{id}/permissions/{perm_id}/` | Удалить разрешение у роли |
| PATCH | `/api/admin/users/{id}/role/` | Изменить роль пользователя |

### Админка Django

Доступна по адресу: http://127.0.0.1:8000/admin/

## 🧪 Тестирование системы

### Тест 1: Обычный пользователь
```bash
# Логин
curl -X POST http://127.0.0.1:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{"email": "simple@example.com", "password": "simple123"}'

# Чтение статей (должно работать)
curl -X GET http://127.0.0.1:8000/api/articles/ \
  -H "Cookie: sessionid=<ваш_session_id>"

# Создание статьи (должна быть 403 ошибка)
curl -X POST http://127.0.0.1:8000/api/articles/create/ \
  -H "Content-Type: application/json" \
  -H "Cookie: sessionid=<ваш_session_id>" \
  -d '{"title": "Новая", "content": "Текст"}'

# Доступ к Admin API (должна быть 403)
curl -X GET http://127.0.0.1:8000/api/admin/roles/ \
  -H "Cookie: sessionid=<ваш_session_id>"
```

### Тест 2: Администратор
```bash
# Логин
curl -X POST http://127.0.0.1:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{"email": "admin@example.com", "password": "admin123"}'

# Все операции доступны, включая Admin API
curl -X GET http://127.0.0.1:8000/api/admin/roles/ \
  -H "Cookie: sessionid=<ваш_session_id>"
```

## 🛡️ Безопасность

- Пароли хранятся в хешированном виде
- Сессионная аутентификация
- CSRF защита (отключена для API, можно включить для production)
- Проверка прав на уровне каждого endpoint
- Мягкое удаление пользователей (`is_active=False`)
- Admin API защищён разрешением `permission.manage`

## 📁 Структура проекта

```
auth_system/
├── config/           # Настройки Django
├── users/            # Приложение пользователей
│   ├── models.py     # Модель User
│   ├── serializers.py # Сериализаторы
│   ├── views.py      # Вьюхи аутентификации
│   └── urls.py       # Маршруты API
├── auth_system/      # Система прав (RBAC)
│   ├── models.py     # Модели Role, Permission, RolePermission
│   ├── serializers.py # Сериализаторы для Admin API
│   ├── views.py      # Вьюхи Admin API
│   ├── urls.py       # Маршруты Admin API
│   └── admin.py      # Админка Django
├── mock_app/         # Mock API для демонстрации
│   ├── views.py      # Вьюхи статей
│   └── urls.py       # Маршруты статей
├── utils/            # Вспомогательные модули
│   └── permissions.py # Класс HasPermission
├── fixtures/         # Тестовые данные
├── manage.py
└── README.md
```

## 🔧 Кастомный класс разрешений

```python
from utils.permissions import HasPermission

@api_view(['GET'])
@permission_classes([HasPermission('article.read')])
def article_list(request):
    """Требуется разрешение article.read"""
    return Response(...)
```

Класс `HasPermission` проверяет:
1. Авторизацию пользователя (401 если нет)
2. Активность аккаунта
3. Наличие роли у пользователя
4. Наличие нужного разрешения у роли (403 если нет)

## 📝 Соответствие требованиям ТЗ

| Требование | Статус |
|------------|--------|
| Регистрация пользователя | ✅ |
| Вход/выход из системы | ✅ |
| Обновление профиля | ✅ |
| Мягкое удаление | ✅ |
| Собственная система прав (не из коробки) | ✅ |
| Описание схемы RBAC | ✅ |
| Таблицы в БД для прав | ✅ |
| Тестовые данные | ✅ |
| Проверка прав (401/403) | ✅ |
| **API для управления правами (для администратора)** | ✅ |
| Mock бизнес-объекты | ✅ |
| Работающая система | ✅ |

## 🚀 Деплой

Для production рекомендуется:
1. Использовать PostgreSQL вместо SQLite
2. Включить CSRF защиту
3. Настроить CORS
4. Использовать JWT вместо сессий для мобильных клиентов
5. Настроить HTTPS

## 👨‍💻 Разработка

### Добавление нового разрешения:

1. Добавить запись в БД через админку или миграцию
2. Использовать в коде:
```python
@permission_classes([HasPermission('новое.разрешение')])
```

### Добавление новой роли:

1. Создать роль в админке
2. Назначить разрешения
3. Назначить роль пользователям

### Использование Admin API:

```bash
# Добавить разрешение роли
curl -X POST http://127.0.0.1:8000/api/admin/roles/1/permissions/ \
  -H "Content-Type: application/json" \
  -H "Cookie: sessionid=<ваш_session_id>" \
  -d '{"permission_id": 1}'

# Изменить роль пользователя
curl -X PATCH http://127.0.0.1:8000/api/admin/users/2/role/ \
  -H "Content-Type: application/json" \
  -H "Cookie: sessionid=<ваш_session_id>" \
  -d '{"role_id": 2}'
```

## 📄 Лицензия 
MIT

## 🤝 Контакты 
[Константин Кононенко] - [kali-krit@ya.ru]
GitHub: [https://github.com/kalikrit/auth-system]